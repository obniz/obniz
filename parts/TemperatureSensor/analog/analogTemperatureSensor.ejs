![photo of AnalogTemperatureSensor](./wired.png)

<%
if (typeof pins === "undefined") {
    pins = {};
}
if (pins.gnd === undefined) {
    pins.gnd = 0;
}
if (pins.output === undefined) {
    pins.output = 1;
}
if (pins.vcc === undefined) {
    pins.vcc = 2;
}
%>


## wired(obniz, {vcc, gnd, output})
```javascript
// Javascript Example
var tempsens = obniz.wired("<%= partname %>", { gnd:<%= pins.gnd %> , output:<%= pins.output %>, vcc:<%= pins.vcc %>});
```

## onchange
callback function for temperature change.
Unit of temp is Celsius

```javascript
// Javascript Example
var tempsens = obniz.wired("<%= partname %>",   { gnd:<%= pins.gnd %> , output:<%= pins.output %>, vcc:<%= pins.vcc %>});
tempsens.onchange = function(temp){
console.log(temp)
};
```

## [await]getWait
get temperature change.
Unit of temp is Celsius

```javascript
// Javascript Example
var tempsens = obniz.wired("<%= partname %>",   { gnd:<%= pins.gnd %> , output:<%= pins.output %>, vcc:<%= pins.vcc %>});
var temp = await tempsens.getWait();
console.log(temp);
```
