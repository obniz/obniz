## YAML Template.
##---
$schema: http://json-schema.org/draft-04/schema#
id: "/request/display"

type: object
additionalProperties: false
properties:
    text: 
        type : string

    clear: 
        type: boolean

    qr : 
        type: object
        required: [data]
        additionalProperties: false
        properties:
            data :
                type : string
            collection :
                type : string
                enum : [L, M, Q, H]
                default : M
    raw :
        type : array
        minItems: 1024   # 128*64 / 8
        maxItems: 1024   # 128*64 / 8
        items :
            type : integer
            minimum : 0
            maximum : 255
            