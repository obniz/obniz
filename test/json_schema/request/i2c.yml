## YAML Template.
##---
$schema: http://json-schema.org/draft-04/schema#
id: "/request/i2c"

"anyOf":
  -
    type: "null"

  -
    type: object
    additionalProperties: false
    properties:
        mode : 
            type : string
            enum : [master] 
        sda :  {$ref:#pinSetting}
        scl :  {$ref:#pinSetting}
        slave_address_length : 
            type : integer
            enum : [7,10]
            default : 7
            
        slave_address: 
            type : integer
            minimum : 0
            
        clock: 
            type: integer
            minimum : 0
                
        address : 
            type: integer
            minimum : 0

        address_bits:
            type: integer
            enum: [7, 10]
            default: 7
            
        data : {$ref:#dataArray}
        read : 
            type: integer
            minimum : 0
            