## YAML Template.
##---
$schema: http://json-schema.org/draft-04/schema#
id: "/request/logicAnalyzer"

"anyOf":
  -
    type: "null"

  -
    type: object
    additionalProperties: false
    required : [io, interval, length]
    properties:
        io : 
            type : array
            items : {$ref :#pinSetting}
            
        interval : 
            type : number
            minimum : 0
        length :
            type : integer
            
        triger :
            type : object
            additionalProperties: false
            required : [value, samples]
            properties:
                value : 
                    type : boolean
                    minimum : 0
                    maximum : 1
                    
                samples :
                    type : integer
            