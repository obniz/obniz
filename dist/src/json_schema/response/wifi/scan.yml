## YAML Template.
##---
$schema: http://json-schema.org/draft-04/schema#
id: "/response/wifi/scan"
desccription: "wifi scan"

type: object
required: [scan]
properties:
  scan:
    type: array
    description: "wifi scan list"
    items:
      type: object
      required: [ssid, macAddress, rssi]
      properties:
        ssid:
          type: string
          example: "obniz-wifi"
        macAddress:
          type: string
          example: "001122aabbcc"
        rssi:
          type: integer
