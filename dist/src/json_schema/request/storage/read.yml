## YAML Template.
##---
$schema: http://json-schema.org/draft-04/schema#
id: "/request/storage/read"
related: "/response/storage/read"
desription: "Read file data by file name from obniz storage."

type: object
required: [read]
properties:
  read:
    type: object
    required: [fileName]
    properties:
      fileName:
        type: string
        example: "launch_img"
