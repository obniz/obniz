## YAML Template.
##---
$schema: http://json-schema.org/draft-04/schema#
id: "/request/system/network_get"

type: object
required: [network]
properties:
  network:
    type: string
    enum:
        - get