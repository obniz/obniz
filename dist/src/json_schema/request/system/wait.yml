## YAML Template.
##---
$schema: http://json-schema.org/draft-04/schema#
id: "/request/system/wait"

type: object
required: [wait]
properties:
  wait:
    type: integer
    description: "wait time (ms)"
