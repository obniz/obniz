## YAML Template.
---
  $schema: http://json-schema.org/draft-04/schema#
  id: "/"
  definitions :
      pinSetting :
          id : "pinSetting"
          type : integer
          minimum : 0
          maximum : 11
          default : null

      dataArray31 :
          id : "dataArray31"
          type : array
          default : null
          maxItems : 31
          items :
              type : integer
              minimum : 0
              maximum : 255

      dataArray1024 :
          id : "dataArray1024"
          type : array
          default : null
          maxItems : 1024
          items :
              type : integer
              minimum : 0
              maximum : 255

      dataArray :
          id : "dataArray"
          type : array
          default : null
          items :
              type : integer
              minimum : 0
              maximum : 255

      hexString :
          id : "hexString"
          type : string
          default : null
          "pattern": "^([0-9a-fA-F]+)$"

      uuid :
          id : "uuid"
          type : string
          pattern: "^([-0-9a-fA-F]+)$"   ## "-"は許容
          minLength: 4
          maxLength: 36

      deviceAddress :
          id : "deviceAddress"
          type : string
          pattern: "^([0-9a-fA-F]+)$"
          minLength: 12
          maxLength: 12

      obnizId :
          id : "obnizId"
          type : [string, integer]
          pattern: "^[0-9]{4}-?[0-9]{4}$"   ## "-"は許容
          minimum: 0
          maximum: 99999999

