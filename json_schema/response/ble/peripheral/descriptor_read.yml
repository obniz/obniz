## YAML Template.
##---
$schema: http://json-schema.org/draft-04/schema#
id: "/response/ble/central/descriptor_read"


type: object
required : [peripheral]
properties:
        peripheral:
            type : object
            additionalProperties: false
            required : [read_descriptor_results]
            properties:

                read_descriptor_results :
                    type : array
                    minItems : 1
                    items :
                        type : object
                        required: [service_uuid, characteristic_uuid,descriptor_uuid,  data]
                        additionalProperties: false
                        properties:
                            service_uuid : {$ref :uuid}
                            characteristic_uuid : {$ref :uuid}
                            descriptor_uuid : {$ref :uuid}
                            data : {$ref :#detaArray}