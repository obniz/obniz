## YAML Template.
##---
$schema: http://json-schema.org/draft-04/schema#
id: "/request/pwm/pulse"

type: object
required : [pulse]
properties:
        pulse:
            type:  number
            multipleOf: 0.001
            description : "pulse width (ms)"
