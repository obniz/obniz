## YAML Template.
##---
$schema: http://json-schema.org/draft-04/schema#
id: "/request/tcp/connect"


type: object
required : [connect]
properties:
    connect :
        type : object
        required: [index,port,domain]
        additionalProperties: false
        properties:
            index:
                type: integer
                enum: [0, 1, 2, 3, 4, 5, 6, 7]
                description : "TCP session index"
            port :
                type: integer
                minimum : 0
                maximum : 65535
                description : "TCP port"
            domain:
                type: string
                maxLength: 30
                description : "Server Domain"
