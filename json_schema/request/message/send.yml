## YAML Template.
##---
$schema: http://json-schema.org/draft-04/schema#
id: "/request/message/send"

type: object
additionalProperties: false
required : [data, to]
properties:
    data : {}   # all OK
    to :
        type : array
        minItems : 1
        items : { $ref : "/obnizId" }