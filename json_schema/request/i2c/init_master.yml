## YAML Template.
##---
$schema: http://json-schema.org/draft-04/schema#
id: "/request/i2c/init_master"

type: object
required : [mode, sda, scl, clock]
properties:
    mode :
        type : string
        enum : [master]

    sda :  {$ref: "/pinSetting"}
    scl :  {$ref: "/pinSetting"}

    clock:
        type: integer
        minimum : 1
        maximum : 1000000
