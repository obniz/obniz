## YAML Template.
##---
$schema: http://json-schema.org/draft-04/schema#
id: "/request/spi/init_master"

type: object
required : [mode, clock]  #clk, miso, mosi are optional
properties:
    mode  :
        type : string
        enum : [master]
    clk :  {$ref: "/pinSetting"}
    mosi :  {$ref: "/pinSetting"}
    miso :  {$ref: "/pinSetting"}
    clock :
        type: number
        default : 115200
        minimum : 1
        maximum : 80000000  # 80MHz
