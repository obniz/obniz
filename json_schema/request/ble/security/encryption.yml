## YAML Template.
##---
$schema: http://json-schema.org/draft-04/schema#
id: "/request/ble/security/encription"
description : "ble encription level settings"


type: object
required : [security]
properties:
    security :
        type : object
        required : [encryption_level]
        properties:
            encryption_level :
                    type: string
                    enum :
                       - none
                       - encryption
                       - mitm